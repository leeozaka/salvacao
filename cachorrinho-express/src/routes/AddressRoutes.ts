import { Router } from 'express';
// import AddressController from 'controllers/AddressController';
// import authorized from 'middlewares/auth';

const routes = Router();
// const addressController = new AddressController();

// routes.post('/create', authorized, addressController.create);
// routes.get('/', authorized, addressController.findAll);
// routes.get('/me', authorized, addressController.find);
// routes.delete('/all', authorized, addressController.deleteAll);
// routes.put('/:id', authorized, addressController.update);
// routes.delete('/:id', authorized, addressController.delete);

export default routes;
